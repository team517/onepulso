node_modules/
data/credentials.json
.env.local

# Output de build de Next.js — NUNCA va a git
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts
dev.log
.vercel

# Archivos generados durante tests locales
big.csv
resp.json
chunk.json
edge.json
evadan-*.json
leads.json
leads-real.json

# Data runtime — TODO. Contiene credenciales reales (IMAP/SMTP passwords),
# leads de clientes, threads de correo, etc. Persistencia en Postgres en prod.
data/*
data/*/
# Pero SÍ mantenemos los .md de memoria (son seed de tu propuesta de valor)
!data/memory/
!data/memory/*.md
